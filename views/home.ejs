<%- include('partials/header'); -%>
<h1>Home</h1>

<% sentPosts.forEach(function(post){ %>
    <div style="display: flex; flex-direction: column; justify-content: space-between;">
        <h2><%= post.title %></h2>
        <p><%= post.body %></p>
    </div>
  
<% }) %>
<%- include('partials/footer'); -%>